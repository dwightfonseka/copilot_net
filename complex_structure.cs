// create a C# console app that generates 10 radom numbers from 50 to 100
// displays the numbers generated in the console
// calculates the sum, mean of the numbers and displays in the console

//   structure
     // objectives , partA does xxx, partE applies to partA/B/C only, input & output
     // dependencies (partC depends on partA) & libraries(specials)
     // reference to other files (dictionary/lookup tables)

//   cross mapping
     // naming convebtions of variables, inputs and outputs
     // this uses output of xxx (source) to create xxx (results)
     // 

//   key words
     // create, implement, modify the C# code
     // debugging based on error
     //

//  optimization performance
     // algorithm choice, refactoring, 
     // file type choice (matrix)
     //  shorten the code

//  error handling
    // try catch
    // input validation
    // calcuation error (divide by 0)

//  security and authentication
    // password / mazimum number of retires
    // API (header) / rate liminig
    // token

//  reduce latency
   // asyncronous
   // caching
   // parallel programming

//  logging and records (a separate file)
   // errors
   // usage (user id)
   // time taken (look for bottlenecks) {start/stop timestamp}

//  documentation
   // API endpoints / SQL endpoints
   // flowchart for data flo
   // data transformation logic (data > aggregation/ modification/ claculations)

// output 
    // razor page (html)
    // console output
    // file saved in project (eg logs)



